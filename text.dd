using sys
@@ 这是一条单行注释
class main{
    5-3-1+1+3
    3-6+6
    use a = 1   @@ 定义一个变量a
    use b=a+4
    out(b)      @@ 输出b的值
    use c=b+b
    out(c)
    c=in()
    out(c)
    out(b)
    out(3+5)
}
class abc{
    use c=3+5
    use name=c+c
    c=name+c
    out(1)
}
class abcd{
    use c=3+5
}